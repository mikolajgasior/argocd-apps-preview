---
apiVersion: v1
kind: ConfigMap
metadata:
  name: example-config
  namespace: tools
  labels:
    app: my‑app
data:
  config.yaml: |
    server:
      port: 8080
    database:
      host: db.example.com
      name: mydb
